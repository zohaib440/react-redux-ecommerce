/*
// redux 
Redux is predictable state container for javascript apps 
So redux is not tied with react only, it is library for javascript applications , 

The <Provider> component makes the Redux store available to any nested components that need to access the Redux store.

// Store 
One store for enitire application, that holds the state of the application, it is created in file store.jsx , it is created by createStore and this method takes parameter as reducer  

 */
